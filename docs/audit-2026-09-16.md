# Audit du projet — 16 septembre 2026

Audit externe demandé à 24 jours de l'échéance. Méthode : lecture intégrale
de la documentation, exécution de la suite de vérification locale, trois
revues de code indépendantes (backend, ML/données, frontend) avec
reproduction des défauts signalés, et vérification sur Internet des règles
officielles, des versions et des travaux concurrents. Les corrections de code
ne sont **pas** appliquées par cet audit ; elles sont priorisées dans
[plan-soumission.md](plan-soumission.md).

## Verdict

Le projet a une gouvernance scientifique rare pour un hackathon : manifestes
et checksums, split groupé verrouillé, protocole d'accès test unique, REX
datés, aucun chiffre non versionné. Ses faiblesses sont ailleurs :

1. deux blocages administratifs (dépôt privé, pas de licence) ;
2. aucun modèle appris n'apporte encore de preuve : le classifieur de qualité
   n'est pas dans le produit et ne bat pas le raccourci d'acquisition ;
3. le split « anti-fuite » laisse passer des séries longitudinales
   (dates consécutives, mêmes lignées) ;
4. le produit casse sur des cas de démo réalistes (TIFF 16 bits, lot > 25 Mo,
   import et analyse couplés) ;
5. la grille d'évaluation documentée n'était pas celle de l'organisateur.

Les points 3 à 5 sont corrigeables en une semaine ; le point 2 est le seul
travail scientifique restant et il doit être borné.

## Vérification locale (exécutée le 16 septembre 2026)

| Contrôle | Résultat |
|---|---|
| `ruff check` (périmètre Makefile) | propre |
| `pytest backend/tests` | 94 réussis, 1 ignoré, 3 avertissements de dépréciation Starlette |
| `npm run typecheck` (TypeScript 7.0.2) | propre |
| `vite build` | 234,7 kB JS (73 kB gzip), 16,2 kB CSS |
| `check_ooc_cnn_notebook.py` | OK |
| `docker compose config --quiet` | OK |

## Blocages administratifs

| Constat | Preuve | Action |
|---|---|---|
| dépôt `oumar5/organchip-insight` **privé**, aucune licence | `gh repo view` : `visibility: PRIVATE`, `licenseInfo: null` | choisir la licence (Apache-2.0 recommandée), ajouter `LICENSE`, rendre public au plus tard à la release |
| grille et calendrier documentés différents de l'organisateur | page officielle `topic_detail/26` : innovation 30 %, achèvement 25 %, valeur 20 %, complétude 15 %, crédibilité 10 % ; finale avec soutenance 20–30 octobre | corrigé dans [competition-requirements.md](competition-requirements.md) |
| image Kaggle probablement en PyTorch 2.11 | base Colab 2026.07 (`Kaggle/docker-python`, FAQ Colab) ; contrat `torch < 2.11`, `torchvision < 0.26` | vérifier sur Kaggle, étendre le contrat après smoke local |

## Constats scientifiques (ML et données)

### Élevé

| # | Constat | Preuve | Recommandation |
|---|---|---|---|
| S1 | **Risque de fuite longitudinale** (formulation amendée le 16 septembre, voir addendum) : le groupe `YYMMDD` sépare des jours, pas des lots de puces. 21 paires de dates distantes de ≤ 3 jours partagent une lignée de part et d'autre d'un split ; 345 des 473 images test appartiennent à une date voisine d'une date train/validation (`230523` train jour 0–1 → `230524` test jour 2–3 → `230525` train, HPMEC). | recalcul depuis `data/splits/ooc-grouped-v1.csv` ; `split_ooc.py:114-193` ; limitations `split_ooc.py:700-719` ne le mentionnent pas | split v2 par « campagnes » (dates ≤ 3 jours et lignée commune fusionnées) **avant** le run GPU ; phrase explicite dans le rapport |
| S2 | **Aucune preuve d'apprentissage au-delà du raccourci** : handcrafted test macro-F1 0,692 contre 0,695 pour mode + résolution seuls ; seule la ROC-AUC diffère (0,803 / 0,714). Le prétraitement CNN amplifie le raccourci : réplication L→RGB (les images RGB sont `good` à 83 % en test contre 39 % pour `L`), réduction 2056×1542 → 224×168 qui efface le signal haute fréquence définissant la qualité. | `reports/ooc-grouped-split-v1.json`, `reports/benchmarks/ooc-handcrafted-image-quality-v1.json`, `preprocessing.py:17-21`, `:41-51` | recadrage commun, niveaux de gris, ≥ 512 px ou tuiles ; balanced accuracy **par mode** comme métrique primaire ; GroupKFold sur les 50 groupes train+validation |
| S3 | **Le CNN n'a jamais tourné en `validation` et rien dans le produit ne classe la qualité** : `runtime.py:21-23` n'enregistre que le moteur adaptatif ; `onnxruntime` absent de `backend/pyproject.toml` ; le notebook contient des `REPLACE_WITH…`. | `backend/app/ml/runtime.py`, `notebooks/ooc-cnn-kaggle.ipynb` | run GPU puis moteur `ooc-image-quality-cnn` via ONNX Runtime, ou QC explicite de repli |

### Moyen

- S4 — le test « gelé » est lisible et déjà évalué hors protocole :
  `data/splits/ooc-grouped-v1.csv` versionne les labels test ;
  `train_ooc_baseline.py:333-344` charge features et labels test dans le
  processus de sélection ; `split_ooc.py:498-511` publie la distribution des
  labels test par mode. Le protocole à reçus ne protège que le CLI CNN.
- S5 — bruit intra-groupe non quantifié : 116 paires dHash ≤ 8 dans le jeu,
  dont 26 à labels contradictoires et 97 à lignées différentes ; plafond de
  performance et indice que les images d'une date viennent des mêmes puces.
- S6 — contrat runtime trop strict : `runtime_contract.py:162-177` exige
  `onnx` et `onnxruntime` même en mode `validation`.
- S7 — notebook : `SOURCE_BUNDLE_SHA256` est calculé sur l'arbre copié et
  filtré ; aucun outil local ne produit le bundle et son hash de façon
  reproductible (formulation amendée, voir addendum) ; `run_cli` avec
  `capture_output=True` perd la stderr des échecs.
- S8 — double sélection sur la même validation (checkpoint puis seuil), IC
  bootstrap sur 9 grappes, aucune métrique train pour la baseline
  (sur-apprentissage ExtraTrees non mesurable).
- S9 — environnements fragmentés : Python 3.13.5 pour les rapports baseline
  et BBBC019, 3.12.14 pour le CNN, smoke lancé depuis
  `data/cache/microsam-env` ; `uv.lock` ne couvre ni le CNN ni µSAM.

### Faible

Trois implémentations divergentes des métriques binaires
(`split_ooc.py`, `train_ooc_baseline.py`, `ooc_cnn/metrics.py`) ;
`benchmark.py:170-171` note 0 une image sans vérité ni prédiction ; masques
`reannotation` BBBC019 inutilisés pour estimer un plafond inter-annotateur ;
archive OoC vérifiée par md5 seul ; `final-eval` re-hache 7 Go inutilement ;
`training/train.py` est un stub mort ; `export.py` accepte un rapport `smoke`.

## Constats produit (backend)

### Élevé

| # | Constat | Preuve | Recommandation |
|---|---|---|---|
| B1 | **TIFF 16 bits rendu blanc** : `convert("L")` écrête au lieu de remettre à l'échelle. Reproduit : même contenu, 0 objet et deux avertissements faux en `I;16`, 1 objet en 8 bits. Les jeux actuels sont en 8 bits, donc les benchmarks restent valides, mais la promesse « import TIFF de microscopie » ne l'est pas. | `pipeline.py:106-107`, `benchmark.py:287-288` | normaliser selon le dtype (`img_as_float`) avant Otsu et pour l'overlay ; test dédié |
| B2 | **Bombe de décompression** : `DecompressionBombError` non interceptée → 500, fichier orphelin compté, puis expérience bloquée en `analyzing` ; un PNG uniforme 10 000² (97 Ko) est accepté et décode 100 Mpx (≈ 50 o/pixel mesurés, ≈ 5 Go). | `experiments.py:50`, `:92`, `:133-144`, `pipeline.py:108` | `MAX_IMAGE_PIXELS` projet, contrôle des dimensions à l'upload, `except Exception` avec passage en `failed` |

### Moyen

- B3 — statut `analyzing` persistant sur toute exception non `ValueError`,
  y compris après redémarrage (aucune réconciliation).
- B4 — nom de fichier > 255 octets → 500 (`unlink` relève `OSError`).
- B5 — limite 25 Mo appliquée après réception complète ; port 8000 publié sur
  l'hôte, contournant nginx ; aucun quota.
- B6 — analyse synchrone ≈ 0,66 s/Mpx : un lot de 20 images 16 Mpx dépasse le
  `proxy_read_timeout` de 120 s ; relances concurrentes non idempotentes.
- B7 — polarité choisie par a priori de couverture 18 % : sur une monocouche
  confluente, les « objets » sont les interstices.
- B8 — `mean_object_area` moyennée avec des zéros pour les images vides ;
  `mean_equivalent_diameter` jamais agrégé.
- B9 — `cp .env.example .env` fixe `ORGANCHIP_DATA_DIR=/data` et casse le
  mode local (`OSError` à l'import) ; `ORGANCHIP_MAX_UPLOAD_MB` non transmis
  par compose.
- B10 — « vérification de contenu » réelle pour PNG seulement (`verify()`
  est un no-op JPEG/TIFF) ; un JPEG tronqué est accepté puis échoue à
  l'analyse.

### Faible

Connexions SQLite jamais fermées ; `PRAGMA foreign_keys` inactif ; anciens
résultats servis après un upload rejeté ; JSON persisté sans migration ;
`GET /artifacts` crée un dossier par effet de bord et force `attachment` ;
Docker en root, `pip install .` sans `uv.lock` (l'image livrée n'est pas la
résolution testée), pas de build d'image en CI, périmètre ruff CI sans
`backend/scripts` ; `resource` importé (Unix seulement) ; identifiants de
moteur du registre (`micro-sam-pretrained`) et du benchmark
(`micro-sam-vit-b-lm-apg`) déconnectés ; validateur de registre re-exécuté à
chaque appel et sur-testé par monkeypatch.

Tests manquants : `GET /results` (200/404), persistance des résultats après
redémarrage, échec d'analyse → `failed`, JPEG/TIFF/16 bits, limite de taille,
CLI `inference.py`, concurrence.

## Constats produit (frontend)

### Critique pour la démo

| # | Constat | Preuve | Recommandation |
|---|---|---|---|
| F1 | rejets d'upload silencieux : `uploadImages` ignore `accepted_files` / `rejected_files` ; analyse lancée à l'aveugle | `client.ts:35-42`, `App.tsx:199-202` | typer et afficher `UploadSummary` |
| F2 | import et inférence couplés : si l'analyse échoue, le clic suivant ré-envoie les fichiers (doublons stockés, métriques faussées) ; input fichier non réinitialisé | `App.tsx:199-204`, `:450-455` | deux actions distinctes, `event.target.value = ""` |
| F3 | nginx `client_max_body_size 25m` sur le corps entier alors que l'UI promet 25 Mo **par fichier** : trois TIFF de 10 Mo → 413, invisible en dev | `nginx.conf:4`, `client.ts:37` | limite ≥ N × fichier ou upload fichier par fichier |

### Élevé

- F4 — erreurs 422 de validation affichées « [object Object] »
  (`client.ts:13-14`) ; bornes backend non reflétées dans le formulaire.
- F5 — course de réponses obsolètes (pas d'`AbortController`), sélection non
  verrouillée pendant l'analyse, nom de l'expérience jamais affiché dans les
  résultats.
- F6 — 17 styles sous le contraste 4,5:1, textes de 9 à 10 px, dont la mise
  en garde « heuristique 0–1 · pas une probabilité » à 2,49:1 ; illisible en
  projection de soutenance.
- F7 — groupes témoin/traité collectés mais jamais utilisés : le formulaire
  suggère une capacité inexistante.
- F8 — aucun export ; `artifacts` et quatre champs par image reçus mais non
  affichés.
- F9 — pas de progression ; statut `analyzing` inobservable sauf blocage.

### Moyen

Messages backend en anglais mélangés au français ; `VITE_API_BASE_URL` de
`.env.example` sans effet (valeur cuite au build) ; nginx sans
`Cache-Control`, gzip ni en-têtes de sécurité, fallback SPA qui renvoie
`index.html` pour un asset disparu ; provenance « Entraînement local :
Aucun » codée en dur ; overlays sans cache-busting ; aucun test ni lint
frontend ; `App.tsx` monolithique (567 lignes) ; focus clavier invisible sur
la zone de dépôt ; « preuves » dans le titre alors que les sorties sont
exploratoires.

## Documentation

Ce qui fonctionne : index unique, règle documentaire explicite, REX datés
avec addendums plutôt que réécritures, chaque chiffre relié à un rapport
versionné, séparation claire produit / recherche / exploitation.

Ce qui manquait ou était faux, et ce que cet audit a changé :

| Problème | Correction |
|---|---|
| grille, calendrier et livrables non conformes à l'organisateur ; finale avec soutenance absente | [competition-requirements.md](competition-requirements.md) réécrit à partir de la page officielle |
| grille dupliquée et divergente dans `research.md` | remplacée par un renvoi |
| aucune synthèse de l'état de l'art ni des concurrents sur le même dataset | [etat-de-l-art.md](etat-de-l-art.md) |
| aucun plan daté jusqu'à la soumission et la finale ; technologies non anticipées | [plan-soumission.md](plan-soumission.md) |
| déclaration des outils d'IA, modèles et licences exigée par le règlement, absente | [declaration-ia-et-licences.md](declaration-ia-et-licences.md) |
| `validation.md` publiait baseline et raccourci sans dire qu'ils sont indiscernables | note ajoutée |
| roadmap sans blocages administratifs ni jalon de finale | [roadmap.md](roadmap.md) réécrite |

Reste à faire côté documentation :

- une version **anglaise** du README et du rapport : le jury est chinois et
  international, le dépôt est entièrement en français ;
- alléger le README : la section CNN (hashes, commandes d'export) relève de
  `backend/experiments/ooc-cnn/README.md` ;
- corriger les affirmations invalidées par cet audit : « vérification de
  contenu » (PNG seulement), « TIFF », « limite configurable », « plausibilité
  spatiale » ;
- ajouter une fiche modèle (`model card`) pour le CNN dès qu'un run éligible
  existe, et un `CITATION.cff` ;
- BBBC038 est annoncé dans trois documents et jamais exécuté : le marquer
  optionnel partout ou le retirer.

## Douze actions dans l'ordre

1. licence et passage du dépôt en public (décision de l'utilisateur) ;
2. vérifier les versions Kaggle, étendre le contrat runtime après smoke local ;
3. split v2 par campagnes, re-mesure des baselines, REX ;
4. prétraitement CNN v2 (recadrage commun, gris, ≥ 512 px), run `validation`
   GPU, métriques par mode ;
5. corriger B1, B2, B3 et F1–F3 avec tests ;
6. lisibilité (F4, F6) et verrou de sélection (F5) ;
7. export JSON/CSV, tableau par image, nom d'expérience dans les résultats ;
8. accès test unique, reçu archivé, moteur CNN ONNX ou QC de repli ;
9. vue de comparaison des moteurs alimentée par `reports/benchmarks/` ;
10. README anglais, corrections des affirmations, `CITATION.cff` ;
11. machine propre, release, vidéo, rapport, Writeup soumis le 9 octobre ;
12. soutenance : diapositives, démo de secours, réponses préparées.

## Addendum du 16 septembre 2026 — revue croisée et correctifs

Une revue indépendante (GPT Astra) a relu cet audit, reproduit ses points
principaux et contesté certaines formulations. Après recoupement, cinq
corrections sont acceptées et intégrées :

1. **S1 : « fuite » devient « risque de fuite ».** Les 21 paires de dates
   voisines et les 345 images test concernées sont confirmées ; en exigeant
   que la lignée de l'image test soit celle partagée avec la date voisine,
   **270 images test** sont exposées (recalculé indépendamment). Ces chiffres
   établissent une proximité d'acquisition, pas l'identité des puces. Le
   split v1 est donc « groupé par date, contrôlé pour les quasi-doublons,
   indépendance biologique non établie ». Le suivi quotidien des mêmes puces,
   mode de production du dataset, rend l'hypothèse « mêmes cultures » très
   probable sans la prouver.
2. **Split v2 pré-enregistré, v1 conservé.** La règle (3 jours, lignée
   commune, fermeture transitive) est écrite avant tout calcul dans
   [protocole-campagnes-v2.md](protocole-campagnes-v2.md) ; l'audit
   structurel (commit `efc0d05`) donne 29 campagnes ; aucun manifeste v2 ni
   score n'est produit ; le choix entre v1 et v2 ne se fera jamais sur les
   scores test. Le test v1 ayant déjà été consulté au niveau agrégé, aucun
   verrou ne le rend « vierge » : cela doit rester écrit dans le rapport.
3. **Gris et haute résolution sont des hypothèses**, à tester par ablation
   sur train/validation avec liste, seeds, budget et critère figés à
   l'avance. L'arbitre reste la balanced accuracy par mode d'acquisition,
   avec effectifs et classes présentes par tranche.
4. **« Un seul run GPU » était une contrainte de budget.** Plusieurs runs de
   validation sont légitimes ; ce qui est figé, c'est le protocole et
   l'accès unique au test final.
5. **S7 : le hash du bundle est calculable localement** avec le même filtrage.
   Le manque réel est l'outil de préparation reproductible du bundle.

Deux positions sont maintenues : une licence explicite reste nécessaire pour
un dépôt public réutilisable, même si le règlement n'en impose aucune ; le
produit ne doit pas revendiquer un comptage cellulaire validé, ce qui
appelle un benchmark d'instances borné (BBBC038, CC0) et la mention
« composantes connexes » dans l'interface, sans en faire un prérequis à la
soumission. Un point est contesté : un Kaggle Notebook public satisfait la
lettre du règlement comme « dépôt », mais pas la reproduction d'une plateforme
FastAPI + React + Docker ; si l'historique Git pose problème, publier un
dépôt neuf à partir d'un instantané de la release.

### Correctifs appliqués depuis l'audit

Commit `d3938a4` (131 tests, lint, types, build et Compose vérifiés le
16 septembre) : B1 (lecture 16 bits sans écrêtage, moteur
`adaptive-segmentation-1.1.0`), B2 (limite de pixels, refus des piles et
flottants, exceptions capturées), B3 (`failed` sur toute exception, reprise
au démarrage), B4 (noms UTF-8 bornés), B9 (`.env.example` et Compose),
B10 (décodage réel avant acceptation), L1–L3 (connexions fermées, clés
étrangères, invalidation des résultats), F1–F5 (bilan d'import, import
séparé de l'analyse, un fichier par requête avec nginx à 26 Mio, erreurs de
validation lisibles, verrou de sélection, réponses obsolètes ignorées, nom
d'expérience affiché), cache-busting des overlays. Commit `008f507` : sonde
Kaggle autonome prête, à exécuter avec GPU activé et Internet désactivé.

Deux remarques sur ces correctifs : l'overlay d'un TIFF 16 bits est construit
sur l'échelle pleine 0–65535, donc presque noir pour des données 12 bits
stockées en 16 bits (la mesure est correcte grâce à la normalisation P1–P99,
seule l'image d'inspection souffre) ; le détail 404 « No completed analysis »
reste en anglais.

Restent ouverts : B5 (contrôle de taille après réception, port 8000 publié,
quotas), B6 (analyse synchrone et délais), B7 (a priori de couverture), B8
(agrégation avec images vides), F6 (contraste et typographie), F7 (groupes
témoin/traité inutilisés), F8 (export et tableau par image), F9
(progression de l'analyse), en-têtes nginx et image Docker, et l'ensemble
des constats scientifiques S2 à S9 hors audit structurel des campagnes.
